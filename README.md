# DeepLearningWithPyTorch

A polished PyTorch repository for exploring and experimenting with **neural network architectures** including **ANN, RNN, LSTM, and CNN**.  
Perfect for learning, prototyping, and benchmarking deep learning models across various datasets.

---

## 🧠 Features

- **ANN (Artificial Neural Networks):** Feedforward networks for regression & classification.  
- **RNN (Recurrent Neural Networks):** Sequence modeling and time-series predictions.  
- **LSTM (Long Short-Term Memory):** Advanced recurrent networks for capturing long-term dependencies.  
- **CNN (Convolutional Neural Networks):** Image classification and computer vision tasks.

---

## ⚡ Getting Started

### Prerequisites
- Python 3.8+
- PyTorch 2.x
- torchvision
- numpy, pandas, matplotlib (optional)

Install dependencies:
```bash
pip install torch torchvision numpy pandas matplotlib
